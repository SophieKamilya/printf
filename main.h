#ifndef _PRINTF_H
#define _PRINTF_H

#include <stdio.h>
#include <stdarg.h>
int dic_num(int dic);
int print_char(int c);
int print_str(char *s);
int _printf(const char *format, ...);
int _putchar(char c);

#endif
