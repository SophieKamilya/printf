#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
int dic_num(int dic);
int print_char(int c);
int print_str(char *s);
int _printf(const char *form, ...);
int _putchar(char c);

#endif
